# ローカル開発用の設定例。値を調整して環境変数として渡すか、direnv等で読み込む
# devcontainer内のPostgreSQL:
DATABASE_URL=postgres://healthapp:healthapp-local@127.0.0.1:5432/healthcare

# CORSで許可するフロントエンドのオリジン(カンマ区切り)
ALLOWED_ORIGINS=http://localhost:5173

# 設定すると書き込み系API(PUT/DELETE)にBearerトークン認証がかかる(任意)
# API_TOKEN=your-secret-token
